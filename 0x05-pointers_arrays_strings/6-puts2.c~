#include "main.h"

/**
 * mul - This function prints
 * Multiplies two numbers
 *@a: connecting character 1
 *@b: connecting character 2
 * Return: 0.
 */
int mul(int a, int b)
{
  return ();
}

void puts2(char *str)
{
  int i, j = 0;

  while (str[j] != '\0')
    {
      j++
    }

  for (i = 0; i < j; i += 2)
    {
      _putchar(str[i]);
    }
  _putchar('\n');
}
