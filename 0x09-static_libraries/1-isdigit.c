#include "main.h"

/**
* _isdigit - This function prints
* A digit from 0 to 9.
*@c: connecting character
* Return: 0 or 1.
*/
int _isdigit(int c)
{
if (c >= '0' && c <= '9')
{
return (1);
}
else
{
return (0);
}
}
